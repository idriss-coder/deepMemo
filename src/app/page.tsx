import InstallButton from "@/app/install-btn";

export default function Home() {
  return (
      <div
          className="min-h-screen bg-gradient-to-b from-green-100 to-blue-100 font-[family-name:var(--font-geist-sans)] flex items-center justify-center">
          <InstallButton/>
      </div>
  );
}